# 希望条件（サンプル）

LLM がスコアを付けるときに読む文章。**ここを直すのが唯一のチューニング作業。**
数字のハードフィルタは `criteria.json` 側。

実際に使うときは `criteria.md` という名前でコピーして中身を自分のものに置き換える
（`criteria.md` は .gitignore してある）。

## 絶対条件

- フルリモート。常駐と明記されたものは対象外。
  「リモート可」「リモートOK」はフルリモートを意味しないので、
  出社頻度が本文から読めるなら必ず数字で指摘する。
- 業務委託（準委任）。正社員前提、正社員化を目標に置くものは対象外。

## 単価

- 目標と下限は `criteria.json` の `targetMonthlyJpy` / `minMonthlyJpy` に数字で書く。
  ここには「どう扱ってほしいか」だけを書く（例: 目標未満でも内容が良ければ検討に残す）。
- 稼働が週3〜4で時間単価に換算すると目標を超えるものは、下限を割っても知らせる
  （`minMonthlyJpyIfShortWeek` と `shortWeekDays` で数字を指定する）。
- 時間単価しか書かれていない案件を月額と混同しない。

## 技術

- 主軸: TypeScript / React / Next.js
- AIプロダクトの中核開発は最も強い加点。
  単に「AI活用歓迎」レベルではなく、LLMの精度・コスト・評価設計に踏み込む案件を上に出す。

## 判断のしかた

点数はこの文章ではなく `lib/scoring.ts` が計算する。
ここには「何を重く見るか」「何を懸念として挙げてほしいか」を書く。

**推測で埋めないこと。** 案件票に書かれていないことは「記載なし」として扱い、
スコアの根拠にしない。
